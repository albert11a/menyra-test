# social-app.js Kernel Refactor - Phase 2 Plan

Status: Phase 2A implemented on branch `refactorapp`.

Generated by static source inspection on 2026-05-08.

## Selected Target

Phase 2A selected Chat.

The implemented boundary is:

- `apps/menyra-social/core/chat/chat-app-runtime-facade.js`

This is a feature-owned facade around the existing chat runtime controller. It
keeps current behavior by continuing to use `core/chat/chat-runtime-cluster.js`
and `core/chat/chat-runtime-controller.js`.

## Why Chat Was Selected

Chat was the safest first runtime responsibility extraction because current
source shows it is not required for signed-out public business/menu cold load,
QR/table menu, cart, checkout, feed rendering, search, map discovery, or owner
menu/admin startup.

Source-verified current behavior:

- `ensureTabDataCore(...)` starts chat thread listeners for authenticated chat
  tab sessions and stops them for signed-out sessions.
- `startLiveListeners(...)` starts authenticated chat thread listeners together
  with profile/following/notification listeners.
- Chat route replay is isolated through `maybeOpenChatFromQuery(...)` and the
  bridge route API.
- Public menu, QR/table, cart, checkout, orders, and public bootstrap do not call
  the chat wrapper group.

## Why Safer Than Other Targets

CRM/Admin was not selected because source shows CRM reaches public business and
profile metadata through `ensureRestaurantPublicMeta(...)`,
`resolveRestaurantStatusFromLead(...)`, lead/customer/staff/business account
state, CEO scope, and business profile hydration. That is wider than a first
kernel extraction should be.

Discovery/map was not selected because it is already dynamically bridged through
`createDiscoveryRuntimeControllerAsync(...)`, while `/search`, `/map`, Leaflet,
marker/list behavior, and public profile navigation are launch-visible surfaces.

Notifications/push/follow were not selected as a separate first target because
push is tied to service-worker route messages and notifications startup. The
existing chat runtime controller currently owns some follow/notification action
methods, so Phase 2A preserves that existing source shape behind the chat facade
instead of reassigning domain ownership.

Public menu, QR, cart, and order were explicitly excluded because they are
launch-critical.

## Responsibility Moved Out Of `social-app.js`

Moved into `chat-app-runtime-facade.js`:

- chat runtime creation via `createChatRuntimeCluster(...)`
- chat constants:
  - `CHAT_MESSAGE_TTL_MS`
  - `CHAT_ATTACHMENT_INLINE_MAX_BYTES`
  - `CHAT_IMAGE_PREVIEW_COMPRESSION_STEPS`
  - `CHAT_MESSAGE_READ_LIMIT`
- chat DOM helper wrappers:
  - capture/restore chat input focus
  - scroll chat messages to bottom
  - autosize textarea wrapper
- chat thread index/storage wrapper methods
- chat message storage/sync wrapper methods
- chat listener wrapper methods
- chat send wrapper including the existing `chat_send` budget trace
- current chat-controller-owned follow/notification/post helper wrappers

## What Stayed In `social-app.js`

`social-app.js` still owns:

- state object identity
- render/setState
- route detection, route sync, pending route state, and popstate replay
- auth/session bootstrap and session runtime cluster creation
- public bootstrap/direct-entry behavior
- public business/menu/QR/table/cart/order behavior
- bridge shell runtime cluster creation
- small same-name adapter functions used by shell, bridge, session, and event
  binders
- follow, notification, and push runtime cluster creation in the existing
  session runtime cluster

## Initial-Load Impact

This is not a real lazy-load reduction yet.

The direct `social-app.js` import surface is smaller because it no longer imports
the many chat utility modules directly. However, `chat-app-runtime-facade.js` is
still statically imported and still creates the existing chat runtime before the
session runtime cluster, so the chat module graph and eager chat setup remain in
initial startup.

Future lazy boundary:

- Make `chat-app-runtime-facade.js` dynamic only after preserving the
  synchronous chat index restore in session data loading.
- Keep authenticated live-listener startup behavior equivalent for chat tab,
  auth restore, push/chat deep links, and logout cleanup.

## Protected Surfaces Checked

Source-checked and intentionally not moved:

- guest `/feed`
- feed gate / city input behavior
- feed scroll and post rendering
- stories row
- post open / like / comment guest gates
- `/search`
- search input/results
- search result click to user/business profile
- `/map` / discovery / Entdecker Karte
- Leaflet/map loading
- marker/list behavior
- navigation from map/discovery to business profile/menu
- public business route `/:slug`
- public menu route `/:slug/menu`
- public menu aliases `/:slug/karte` and `/:slug/speisekarte`
- QR/table route `/:slug/menu?src=qr&table=1`
- product detail open/close
- category rendering
- product image rendering
- price/currency formatting
- menu item like/comment/favorite guest gates
- cart add/update/remove
- cart badge/FAB
- checkout/pre-submit
- order submit/retry path
- login/register/auth restore
- owner profile
- owner menu/admin view
- CEO/leads/customers/staff/business accounts
- notifications route and notification targets
- follow behavior
- upload/media/story posting
- route sync
- browser back/forward/popstate
- reload/pageshow behavior
- mobile Safari/iOS body-scroll/modal behavior
- Android Chrome behavior
- global `window.__MENYRA_*` contracts
- `bridgeShellRuntimeCluster` bindings
- `controller-deps-factory` wiring
- `sessionDataRuntimeController` loading
- `sessionRuntimeCluster` lifecycle
- listener cleanup/startup behavior outside the chat facade adapter boundary
- localStorage/sessionStorage keys
- Firebase Auth/Firestore reads/writes/listeners

The selected target does touch chat route and chat modal adapters by keeping the
same function names and delegating them to the facade. No routes, Firebase paths,
query shapes, storage keys, or listener semantics were changed.

## Manual Test Checklist

Albert should manually check:

- Signed-out `/feed` still opens, city gate/search feed behavior still works,
  stories render, and post guest gates still prompt as before.
- `/search` and `/map` still load, search/map results open the same
  user/business profile/menu paths, and Leaflet still initializes.
- Public `/:slug`, `/:slug/menu`, `/:slug/karte`, `/:slug/speisekarte`, and
  `/:slug/menu?src=qr&table=1` still render the same visible profile/menu,
  product detail, cart badge/FAB, and checkout behavior.
- Login/register/auth restore still starts the same session and logout still
  clears chat listeners.
- Chat tab loads existing thread list, opens a thread, sends a message, handles
  attachments, closes modal/back navigation, and archives/deletes a thread.
- Notification route/post/chat targets still open the expected target.
- Follow/unfollow and follow-request accept behavior still match current
  behavior.
- Owner profile, owner menu/admin, leads/customers/staff/business accounts, and
  upload/story posting still work as before.
- Browser back/forward, reload/pageshow, iOS modal/body scroll, and Android
  Chrome behavior have no visible regression.

## Rollback

If Phase 2A is the current tip:

```bash
git revert HEAD
```

If later commits exist, revert the specific Phase 2A commit hash reported with
the implementation.
