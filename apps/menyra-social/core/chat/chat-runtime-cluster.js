import { createChatRuntimeController } from "./chat-runtime-controller.js";

export function createChatRuntimeCluster({
  stateDeps = {},
  constants = {},
  firebaseApi = {},
  renderApi = {},
  followApi = {},
  stateApi = {},
  uiApi = {}
} = {}) {
  return createChatRuntimeController({
    state: stateDeps.state,
    safeStorage: stateDeps.safeStorage,
    STORAGE_KEYS: constants.STORAGE_KEYS,
    chatIndexKey: stateDeps.chatIndexKey,
    toDateSafe: constants.toDateSafe,
    normalizeHandle: constants.normalizeHandle,
    normalizeFollowHandle: constants.normalizeFollowHandle,
    compressImage: constants.compressImage,
    CHAT_ATTACHMENT_INLINE_MAX_BYTES: constants.CHAT_ATTACHMENT_INLINE_MAX_BYTES,
    CHAT_MESSAGE_TTL_MS: constants.CHAT_MESSAGE_TTL_MS,
    CHAT_IMAGE_PREVIEW_COMPRESSION_STEPS: constants.CHAT_IMAGE_PREVIEW_COMPRESSION_STEPS,
    CHAT_MESSAGE_READ_LIMIT: constants.CHAT_MESSAGE_READ_LIMIT,
    db: firebaseApi.db,
    collection: firebaseApi.collection,
    query: firebaseApi.query,
    orderBy: firebaseApi.orderBy,
    where: firebaseApi.where,
    limit: firebaseApi.limit,
    onSnapshot: firebaseApi.onSnapshot,
    doc: firebaseApi.doc,
    getDoc: firebaseApi.getDoc,
    getDocs: firebaseApi.getDocs,
    setDoc: firebaseApi.setDoc,
    updateDoc: firebaseApi.updateDoc,
    deleteDoc: firebaseApi.deleteDoc,
    increment: firebaseApi.increment,
    serverTimestamp: firebaseApi.serverTimestamp,
    runTransaction: firebaseApi.runTransaction,
    currentUserBadge: renderApi.currentUserBadge,
    render: renderApi.render,
    renderOverlays: renderApi.renderOverlays,
    updateNotificationBadges: renderApi.updateNotificationBadges,
    saveNotifications: renderApi.saveNotifications,
    updateNotificationsDom: renderApi.updateNotificationsDom,
    openPostModal: renderApi.openPostModal,
    openChatWithProfile: renderApi.openChatWithProfile,
    openProfileFromUser: renderApi.openProfileFromUser,
    openGuestAuthPrompt: renderApi.openGuestAuthPrompt,
    applyFollowingHandles: followApi.applyFollowingHandles,
    getFollowDocId: followApi.getFollowDocId,
    isLocalBusinessProfile: followApi.isLocalBusinessProfile,
    saveFollowing: followApi.saveFollowing,
    businessProfileCache: followApi.businessProfileCache,
    findPostById: followApi.findPostById,
    normalizeFeedPost: followApi.normalizeFeedPost,
    pushUserNotification: followApi.pushUserNotification,
    pushUserNotificationWithId: followApi.pushUserNotificationWithId,
    getPendingCommentHighlight: stateApi.getPendingCommentHighlight,
    setPendingCommentHighlight: stateApi.setPendingCommentHighlight,
    getLastRenderMode: stateApi.getLastRenderMode,
    escapeHtml: uiApi.escapeHtml,
    icon: uiApi.icon,
    formatRelative: uiApi.formatRelative,
    getOptimizedImageUrl: uiApi.getOptimizedImageUrl,
    getDocumentObj: stateApi.getDocumentObj,
    getWindowObj: stateApi.getWindowObj,
    alertFn: uiApi.alertFn,
    queueMicrotaskFn: uiApi.queueMicrotaskFn,
    setTimeoutFn: uiApi.setTimeoutFn
  });
}
