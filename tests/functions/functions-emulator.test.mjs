import test from "node:test";

test.skip("functions emulator exposes Heart/admin handlers against local project only", async () => {});
test.skip("order-related functions reject non-local test payloads without emulator context", async () => {});
